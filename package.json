{
  "name": "kevin-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-search-index.js && node scripts/ensure-analytics.js"
  },
  "dependencies": {
    "stripe": "^17.0.0",
    "@upstash/redis": "^1.34.0",
    "resend": "^4.0.0"
  }
}
